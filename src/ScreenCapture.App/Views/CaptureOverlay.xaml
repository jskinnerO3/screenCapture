<Window x:Class="ScreenCapture.App.Views.CaptureOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowState="Normal"
        Cursor="Cross">

    <Grid>
        <!-- Darkened background -->
        <Rectangle x:Name="DarkOverlay" Fill="#80000000"/>

        <!-- Selection rectangle (will be positioned via code) -->
        <Canvas x:Name="SelectionCanvas">
            <!-- Clear area showing through the dark overlay -->
            <Rectangle x:Name="SelectionRect"
                       Stroke="#2196F3"
                       StrokeThickness="2"
                       Fill="Transparent"
                       Visibility="Collapsed"/>

            <!-- Size indicator -->
            <Border x:Name="SizeIndicator"
                    Background="#E0000000"
                    CornerRadius="4"
                    Padding="8,4"
                    Visibility="Collapsed">
                <TextBlock x:Name="SizeText"
                           Foreground="White"
                           FontSize="12"/>
            </Border>
        </Canvas>

        <!-- Instructions -->
        <Border VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,50,0,0"
                Background="#E0000000" CornerRadius="8" Padding="16,8">
            <TextBlock Text="Click and drag to select region. Press ESC to cancel."
                       Foreground="White" FontSize="14"/>
        </Border>
    </Grid>
</Window>
